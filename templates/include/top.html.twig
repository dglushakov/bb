<div class="row bg-dark text-white">
    <div class="col-5">

    </div>
    <div class="col-5">

    </div>
    <div class="col-2">
        <div class="row">
            <div class="col">
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="text-white">logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-white">login</a>
                {% endif %}
            </div>
        </div>
        <div class="row">
            <div class="col">
                You logged as:
                {{ app.user.username|default('nobody') }}
            </div>
        </div>

    </div>
</div>